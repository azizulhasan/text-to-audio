/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!*********************************************!*\
  !*** ./admin/js/text-to-audio-dashboard.js ***!
  \*********************************************/
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _createForOfIteratorHelper(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (!it) { if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e) { throw _e; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var normalCompletion = true, didErr = false, err; return { s: function s() { it = it.call(o); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e2) { didErr = true; err = _e2; }, f: function f() { try { if (!normalCompletion && it["return"] != null) it["return"](); } finally { if (didErr) throw err; } } }; }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
function _regeneratorRuntime() { "use strict"; /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/facebook/regenerator/blob/main/LICENSE */ _regeneratorRuntime = function _regeneratorRuntime() { return exports; }; var exports = {}, Op = Object.prototype, hasOwn = Op.hasOwnProperty, $Symbol = "function" == typeof Symbol ? Symbol : {}, iteratorSymbol = $Symbol.iterator || "@@iterator", asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator", toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag"; function define(obj, key, value) { return Object.defineProperty(obj, key, { value: value, enumerable: !0, configurable: !0, writable: !0 }), obj[key]; } try { define({}, ""); } catch (err) { define = function define(obj, key, value) { return obj[key] = value; }; } function wrap(innerFn, outerFn, self, tryLocsList) { var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator, generator = Object.create(protoGenerator.prototype), context = new Context(tryLocsList || []); return generator._invoke = function (innerFn, self, context) { var state = "suspendedStart"; return function (method, arg) { if ("executing" === state) throw new Error("Generator is already running"); if ("completed" === state) { if ("throw" === method) throw arg; return doneResult(); } for (context.method = method, context.arg = arg;;) { var delegate = context.delegate; if (delegate) { var delegateResult = maybeInvokeDelegate(delegate, context); if (delegateResult) { if (delegateResult === ContinueSentinel) continue; return delegateResult; } } if ("next" === context.method) context.sent = context._sent = context.arg;else if ("throw" === context.method) { if ("suspendedStart" === state) throw state = "completed", context.arg; context.dispatchException(context.arg); } else "return" === context.method && context.abrupt("return", context.arg); state = "executing"; var record = tryCatch(innerFn, self, context); if ("normal" === record.type) { if (state = context.done ? "completed" : "suspendedYield", record.arg === ContinueSentinel) continue; return { value: record.arg, done: context.done }; } "throw" === record.type && (state = "completed", context.method = "throw", context.arg = record.arg); } }; }(innerFn, self, context), generator; } function tryCatch(fn, obj, arg) { try { return { type: "normal", arg: fn.call(obj, arg) }; } catch (err) { return { type: "throw", arg: err }; } } exports.wrap = wrap; var ContinueSentinel = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} var IteratorPrototype = {}; define(IteratorPrototype, iteratorSymbol, function () { return this; }); var getProto = Object.getPrototypeOf, NativeIteratorPrototype = getProto && getProto(getProto(values([]))); NativeIteratorPrototype && NativeIteratorPrototype !== Op && hasOwn.call(NativeIteratorPrototype, iteratorSymbol) && (IteratorPrototype = NativeIteratorPrototype); var Gp = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(IteratorPrototype); function defineIteratorMethods(prototype) { ["next", "throw", "return"].forEach(function (method) { define(prototype, method, function (arg) { return this._invoke(method, arg); }); }); } function AsyncIterator(generator, PromiseImpl) { function invoke(method, arg, resolve, reject) { var record = tryCatch(generator[method], generator, arg); if ("throw" !== record.type) { var result = record.arg, value = result.value; return value && "object" == _typeof(value) && hasOwn.call(value, "__await") ? PromiseImpl.resolve(value.__await).then(function (value) { invoke("next", value, resolve, reject); }, function (err) { invoke("throw", err, resolve, reject); }) : PromiseImpl.resolve(value).then(function (unwrapped) { result.value = unwrapped, resolve(result); }, function (error) { return invoke("throw", error, resolve, reject); }); } reject(record.arg); } var previousPromise; this._invoke = function (method, arg) { function callInvokeWithMethodAndArg() { return new PromiseImpl(function (resolve, reject) { invoke(method, arg, resolve, reject); }); } return previousPromise = previousPromise ? previousPromise.then(callInvokeWithMethodAndArg, callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg(); }; } function maybeInvokeDelegate(delegate, context) { var method = delegate.iterator[context.method]; if (undefined === method) { if (context.delegate = null, "throw" === context.method) { if (delegate.iterator["return"] && (context.method = "return", context.arg = undefined, maybeInvokeDelegate(delegate, context), "throw" === context.method)) return ContinueSentinel; context.method = "throw", context.arg = new TypeError("The iterator does not provide a 'throw' method"); } return ContinueSentinel; } var record = tryCatch(method, delegate.iterator, context.arg); if ("throw" === record.type) return context.method = "throw", context.arg = record.arg, context.delegate = null, ContinueSentinel; var info = record.arg; return info ? info.done ? (context[delegate.resultName] = info.value, context.next = delegate.nextLoc, "return" !== context.method && (context.method = "next", context.arg = undefined), context.delegate = null, ContinueSentinel) : info : (context.method = "throw", context.arg = new TypeError("iterator result is not an object"), context.delegate = null, ContinueSentinel); } function pushTryEntry(locs) { var entry = { tryLoc: locs[0] }; 1 in locs && (entry.catchLoc = locs[1]), 2 in locs && (entry.finallyLoc = locs[2], entry.afterLoc = locs[3]), this.tryEntries.push(entry); } function resetTryEntry(entry) { var record = entry.completion || {}; record.type = "normal", delete record.arg, entry.completion = record; } function Context(tryLocsList) { this.tryEntries = [{ tryLoc: "root" }], tryLocsList.forEach(pushTryEntry, this), this.reset(!0); } function values(iterable) { if (iterable) { var iteratorMethod = iterable[iteratorSymbol]; if (iteratorMethod) return iteratorMethod.call(iterable); if ("function" == typeof iterable.next) return iterable; if (!isNaN(iterable.length)) { var i = -1, next = function next() { for (; ++i < iterable.length;) if (hasOwn.call(iterable, i)) return next.value = iterable[i], next.done = !1, next; return next.value = undefined, next.done = !0, next; }; return next.next = next; } } return { next: doneResult }; } function doneResult() { return { value: undefined, done: !0 }; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, define(Gp, "constructor", GeneratorFunctionPrototype), define(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = define(GeneratorFunctionPrototype, toStringTagSymbol, "GeneratorFunction"), exports.isGeneratorFunction = function (genFun) { var ctor = "function" == typeof genFun && genFun.constructor; return !!ctor && (ctor === GeneratorFunction || "GeneratorFunction" === (ctor.displayName || ctor.name)); }, exports.mark = function (genFun) { return Object.setPrototypeOf ? Object.setPrototypeOf(genFun, GeneratorFunctionPrototype) : (genFun.__proto__ = GeneratorFunctionPrototype, define(genFun, toStringTagSymbol, "GeneratorFunction")), genFun.prototype = Object.create(Gp), genFun; }, exports.awrap = function (arg) { return { __await: arg }; }, defineIteratorMethods(AsyncIterator.prototype), define(AsyncIterator.prototype, asyncIteratorSymbol, function () { return this; }), exports.AsyncIterator = AsyncIterator, exports.async = function (innerFn, outerFn, self, tryLocsList, PromiseImpl) { void 0 === PromiseImpl && (PromiseImpl = Promise); var iter = new AsyncIterator(wrap(innerFn, outerFn, self, tryLocsList), PromiseImpl); return exports.isGeneratorFunction(outerFn) ? iter : iter.next().then(function (result) { return result.done ? result.value : iter.next(); }); }, defineIteratorMethods(Gp), define(Gp, toStringTagSymbol, "Generator"), define(Gp, iteratorSymbol, function () { return this; }), define(Gp, "toString", function () { return "[object Generator]"; }), exports.keys = function (object) { var keys = []; for (var key in object) keys.push(key); return keys.reverse(), function next() { for (; keys.length;) { var key = keys.pop(); if (key in object) return next.value = key, next.done = !1, next; } return next.done = !0, next; }; }, exports.values = values, Context.prototype = { constructor: Context, reset: function reset(skipTempReset) { if (this.prev = 0, this.next = 0, this.sent = this._sent = undefined, this.done = !1, this.delegate = null, this.method = "next", this.arg = undefined, this.tryEntries.forEach(resetTryEntry), !skipTempReset) for (var name in this) "t" === name.charAt(0) && hasOwn.call(this, name) && !isNaN(+name.slice(1)) && (this[name] = undefined); }, stop: function stop() { this.done = !0; var rootRecord = this.tryEntries[0].completion; if ("throw" === rootRecord.type) throw rootRecord.arg; return this.rval; }, dispatchException: function dispatchException(exception) { if (this.done) throw exception; var context = this; function handle(loc, caught) { return record.type = "throw", record.arg = exception, context.next = loc, caught && (context.method = "next", context.arg = undefined), !!caught; } for (var i = this.tryEntries.length - 1; i >= 0; --i) { var entry = this.tryEntries[i], record = entry.completion; if ("root" === entry.tryLoc) return handle("end"); if (entry.tryLoc <= this.prev) { var hasCatch = hasOwn.call(entry, "catchLoc"), hasFinally = hasOwn.call(entry, "finallyLoc"); if (hasCatch && hasFinally) { if (this.prev < entry.catchLoc) return handle(entry.catchLoc, !0); if (this.prev < entry.finallyLoc) return handle(entry.finallyLoc); } else if (hasCatch) { if (this.prev < entry.catchLoc) return handle(entry.catchLoc, !0); } else { if (!hasFinally) throw new Error("try statement without catch or finally"); if (this.prev < entry.finallyLoc) return handle(entry.finallyLoc); } } } }, abrupt: function abrupt(type, arg) { for (var i = this.tryEntries.length - 1; i >= 0; --i) { var entry = this.tryEntries[i]; if (entry.tryLoc <= this.prev && hasOwn.call(entry, "finallyLoc") && this.prev < entry.finallyLoc) { var finallyEntry = entry; break; } } finallyEntry && ("break" === type || "continue" === type) && finallyEntry.tryLoc <= arg && arg <= finallyEntry.finallyLoc && (finallyEntry = null); var record = finallyEntry ? finallyEntry.completion : {}; return record.type = type, record.arg = arg, finallyEntry ? (this.method = "next", this.next = finallyEntry.finallyLoc, ContinueSentinel) : this.complete(record); }, complete: function complete(record, afterLoc) { if ("throw" === record.type) throw record.arg; return "break" === record.type || "continue" === record.type ? this.next = record.arg : "return" === record.type ? (this.rval = this.arg = record.arg, this.method = "return", this.next = "end") : "normal" === record.type && afterLoc && (this.next = afterLoc), ContinueSentinel; }, finish: function finish(finallyLoc) { for (var i = this.tryEntries.length - 1; i >= 0; --i) { var entry = this.tryEntries[i]; if (entry.finallyLoc === finallyLoc) return this.complete(entry.completion, entry.afterLoc), resetTryEntry(entry), ContinueSentinel; } }, "catch": function _catch(tryLoc) { for (var i = this.tryEntries.length - 1; i >= 0; --i) { var entry = this.tryEntries[i]; if (entry.tryLoc === tryLoc) { var record = entry.completion; if ("throw" === record.type) { var thrown = record.arg; resetTryEntry(entry); } return thrown; } } throw new Error("illegal catch attempt"); }, delegateYield: function delegateYield(iterable, resultName, nextLoc) { return this.delegate = { iterator: values(iterable), resultName: resultName, nextLoc: nextLoc }, "next" === this.method && (this.arg = undefined), ContinueSentinel; } }, exports; }
function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }
function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }
var ttaGetData = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator( /*#__PURE__*/_regeneratorRuntime().mark(function _callee() {
    var url,
      data,
      response,
      responseData,
      _args = arguments;
    return _regeneratorRuntime().wrap(function _callee$(_context) {
      while (1) switch (_context.prev = _context.next) {
        case 0:
          url = _args.length > 0 && _args[0] !== undefined ? _args[0] : '';
          data = _args.length > 1 && _args[1] !== undefined ? _args[1] : {};
          _context.next = 4;
          return fetch(url, {
            // headers: {
            //   "Content-Type": "application/json",
            // },
            method: 'POST',
            // *GET, POST, PUT, DELETE, etc.
            body: data // body data type must match "Content-Type" header
          });
        case 4:
          response = _context.sent;
          _context.next = 7;
          return response.json();
        case 7:
          responseData = _context.sent;
          return _context.abrupt("return", responseData);
        case 9:
        case "end":
          return _context.stop();
      }
    }, _callee);
  }));
  return function ttaGetData() {
    return _ref.apply(this, arguments);
  };
}();
var TTA = {
  speechSynthesis: true,
  utterence: new SpeechSynthesisUtterance(),
  speechRecognitionIsActive: true,
  speechRecognition: window.speechRecognition || window.webkitSpeechRecognition,
  recordStatus: 'record',
  listenStatus: 'listen',
  noticeClass: 'tta_notice',
  timer: null,
  // pauseResumeTimer: () => {
  // 	speechSynthesis.pause();
  // 	//IMPORTANT!! Do not remove: Logging the object out fixes some onend firing issues.
  // 	console.log(TTA.utterence);
  // 	// Placing the speak invocation inside a callback fixes ordering and onend issues
  // 	setTimeout(() => {
  // 		speechSynthesis.resume();
  // 	}, 0);

  // 	timer = setTimeout(TTA.pauseResumeTimer, 10000)
  // },
  buttonTextArr: text_to_audio_obj.buttonTextArr,
  playButtonText: function playButtonText() {
    return this.buttonTextArr.listen_text;
  },
  playButtonContent: function playButtonContent() {
    return '<div style="display:flex;justify-content:center;align-items:center;"><span class="dashicons dashicons-controls-play"></span> <span> ' + this.playButtonText() + '<span></span></span></div>';
  },
  replayButtonText: function replayButtonText() {
    return this.buttonTextArr.replay_text;
  },
  replayButtonContent: function replayButtonContent() {
    return '<div style="display:flex;justify-content:center;align-items:center;"><span class="dashicons dashicons-image-rotate"></span> <span> ' + this.replayButtonText() + '<span></span></span></div>';
  },
  pauseButtonText: function pauseButtonText() {
    return this.buttonTextArr.pause_text;
  },
  pauseButtonContent: function pauseButtonContent() {
    return '<div style="display:flex;justify-content:center;align-items:center;"><span class="dashicons dashicons-controls-pause"></span> <span> ' + this.pauseButtonText() + '<span></span></span></div>';
  },
  resumeButtonText: function resumeButtonText() {
    return this.buttonTextArr.resume_text;
  },
  resumeButtonContent: function resumeButtonContent() {
    return '<div style="display:flex;justify-content:center;align-items:center;"><span class="dashicons dashicons-controls-play"></span> <span> ' + this.buttonTextArr.resume_text + '<span></span></span></div>';
  },
  recordStartButtonContent: function recordStartButtonContent() {
    return '<span class="dashicons dashicons-controls-volumeoff"></span> ' + this.buttonTextArr.start_text;
  },
  recordStopButtonConten: function recordStopButtonConten() {
    return '<span class="dashicons dashicons-controls-volumeon"></span> ' + this.buttonTextArr.stop_text;
  },
  displayApiMissing: function displayApiMissing() {
    var button_id = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : '';
    var is_dashboard = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
    var notice = '';
    var link = '';
    if (!this.TTA.speechRecognitionIsActive) {
      notice += 'Text To Audio: Please enable speechRecognition';
    }
    if (!this.speechSynthesis) {
      if (notice) {
        notice += ' , speechSynthesis.';
      } else {
        notice += 'Text To Audio: Please enable speechSynthesis.';
      }
    }
    if (button_id) {
      var previousSibling = document.getElementById(button_id).previousSibling;
      if (previousSibling) {
        notice += " Click here to <a href=\"https://wordpress.org/plugins/text-to-audio/#how%20to%20enable%20%60%60speechsynthesis%60%60%20on%20firefox%3F\" target=\"_blank\">enable</a>";
        previousSibling.style.display = 'block';
        previousSibling.innerHTML = notice;
        setTimeout(function () {
          document.querySelector('.tta_notice').style.display = 'none';
          previousSibling.innerHTML = '';
        }, 5000);
      } else {
        link += text_to_audio_obj.admin_url + 'admin.php?page=text-to-audio#/docs';
        notice += "\nFollow this link to enable: \n".concat(link);
        alert(notice);
      }
    } else {
      if (is_dashboard) {
        link += text_to_audio_obj.admin_url + 'admin.php?page=text-to-audio#/docs';
      } else {
        if (location.search === '?page=text-to-audio' && location.hash === '#/customize') {
          link += text_to_audio_obj.admin_url + 'admin.php?page=text-to-audio#/docs';
        } else {
          link += 'https://wordpress.org/plugins/text-to-audio/#how%20to%20enable%20%60%60speechsynthesis%60%60%20on%20firefox%3F';
        }
      }
      notice += "\nFollow this link to enable: \n".concat(link);
      alert(notice);
    }
    throw new Error(notice);
  }
};
var SpeechGrammarList = window.SpeechGrammarList || window.webkitSpeechGrammarList;
var SpeechGrammar = window.SpeechGrammar || window.webkitSpeechGrammar;
var SpeechRecognitionEvent = window.SpeechRecognitionEvent || window.webkitSpeechRecognitionEvent;

/**
 * Check if speechRecognition, speechSynthesis is definded in FireFox.
 * If not then show alert for enabling them.
 *
 */
if (TTA.speechRecognition == undefined) {
  TTA.speechRecognitionIsActive = false;
} else {
  var recognition = new TTA.speechRecognition();
  var grammar = '#JSGF V1.0; grammar colors; public <color> = aqua | azure | beige | bisque | black | blue | brown | chocolate | coral | crimson | cyan | fuchsia | ghostwhite | gold | goldenrod | gray | green | indigo | ivory | khaki | lavender | lime | linen | magenta | maroon | moccasin | navy | olive | orange | orchid | peru | pink | plum | purple | red | salmon | sienna | silver | snow | tan | teal | thistle | tomato | turquoise | violet | white | yellow ;';
  var speechRecognitionList = new SpeechGrammarList();
  speechRecognitionList.addFromString(grammar, 1);
  recognition.grammars = speechRecognitionList;
  var newGrammar = new SpeechGrammar();
  newGrammar.src = '#JSGF V1.0; grammar names; public <name> = chris | kirsty | mike;';
  speechRecognitionList[1] = newGrammar; // should add the new SpeechGrammar object to the list.

  /**
   * Get recording settings.
   */
  var recordData = new FormData();
  var recordSettings = {};
  recordData.append('method', 'get');
  ttaGetData(text_to_audio_obj.json_url + 'tta/v1/record', recordData).then(function (res) {
    recordSettings = res.data;
    recognition.continuous = recordSettings.is_record_continously ? recordSettings.is_record_continously : true;
    recognition.lang = recordSettings.tta__recording__lang ? recordSettings.tta__recording__lang : 'en-US';
    localStorage.setItem('tta__sentence_delimiter', recordSettings.tta__sentence_delimiter);
  })["catch"](function (err) {
    console.log(err);
  });
  recognition.interimResults = false;
  recognition.maxAlternatives = 2;

  /**
   *
   * restart recording
   */
  recognition.onsoundend = function () {
    TTA.recordStatus = 'record';
    var record_btn = document.getElementById('tta__start__record');
    if (record_btn) record_btn.innerHTML = TTA.recordStartButtonContent();
  };
}

// Listen content.
if (window.speechSynthesis == undefined) {
  TTA.speechSynthesis = false;
} else {
  TTA.utterence = new SpeechSynthesisUtterance();
}

/**
 *
 * @param {*} current_listening_content_id
 */

window.onload = function () {
  localStorage.setItem('recordStarted', false);
  ttaSetCurrentRecordContentId();
  if (window.speechSynthesis) speechSynthesis.cancel();
};
window.onload();

/**
 * Start recording.
 * @param {string} current_record_content_id
 */
function ttaStartRecording() {
  var current_record_content_id = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : '';
  var tta__sentence_delimiter = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '.';
  if (current_record_content_id !== 'comment' && current_record_content_id !== 'tta__demo_text_for_play') {
    ttaSetCurrentRecordContentId();
  }
  current_record_content_id = localStorage.getItem('current_recording_content_id');
  /**
   * Stop listening before recording.
   */
  if (TTA.speechSynthesis) {
    speechSynthesis.cancel();
  }
  if (!TTA.speechRecognitionIsActive) {
    if (document.getElementsByClassName('wp-block-post-title')) {
      TTA.displayApiMissing('', true);
    } else {
      TTA.displayApiMissing();
    }
  }
  /**
   * Get current recording element
   */
  var current_recording_element = ttaGetCurrrentRecordingElement(current_record_content_id);
  if (current_recording_element === false) {
    alert('Please add a paragraph tag then start recording.');
    return;
  }

  /**
   * Change voice recognition text and icon based on condition.
   */
  ttaChangeRecordButtonText();

  /**
   * Show current recorded text.
   */
  ttaShowRecordedContent(current_record_content_id, tta__sentence_delimiter, current_recording_element);
}
function ttaSetCurrentRecordContentId() {
  if (text_to_audio_obj.classic_editor_is_active) {
    localStorage.setItem('current_listening_content_id', 'content_ifr');
    localStorage.setItem('current_recording_content_id', 'content_ifr');
  } else if (document.getElementsByClassName('wp-block-post-title')) {
    /**
     * Get last paragraph tag id for pasting voice text.
     */
    var blockEditorContent = document.getElementsByClassName('block-editor-block-list__layout');
    if (blockEditorContent[0]) {
      var _iterator = _createForOfIteratorHelper(blockEditorContent[0].children),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          child = _step.value;
          if (child.tagName === 'P') {
            localStorage.setItem('current_recording_content_id', child.getAttribute('id'));
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }
  }
}

/**
 * Show current recorded text.
 * @param {*} current_text
 * @param {*} tta__sentence_delimiter
 * @param {*} current_recording_element
 */
function ttaShowRecordedContent(current_record_content_id, tta__sentence_delimiter, current_recording_element) {
  var current_text = '';
  // let final_transcript = "";
  recognition.onresult = function (event) {
    var event__length = event.results.length;
    if (event.results[event__length - 1].isFinal) {
      current_text = event.results[event__length - 1][0].transcript + ttaShouldAddDelimiter(tta__sentence_delimiter);
      current_text = ttaCaptalizeString(current_text);
    }

    /**
     * Customize page id.
     */
    if (current_record_content_id == 'tta__demo_text_for_play') {
      var previous_text = current_recording_element.value;
      current_recording_element.value = previous_text + current_text;
    } else {
      var _previous_text = current_recording_element.innerHTML;
      current_recording_element.innerHTML = _previous_text + current_text;
    }
  };
}

/**
 * Should add delimiter.
 */
function ttaShouldAddDelimiter(tta__sentence_delimiter) {
  if ((window.navigator.userAgent.indexOf('Opera') || window.navigator.userAgent.indexOf('OPR')) != -1) {
    return tta__sentence_delimiter;
  } else if (window.navigator.userAgent.indexOf('Edg') != -1) {
    return '';
  } else if (window.navigator.userAgent.indexOf('Chrome') != -1) {
    return tta__sentence_delimiter;
  } else if (window.navigator.userAgent.indexOf('Safari') != -1) {
    return tta__sentence_delimiter;
  } else if (window.navigator.userAgent.indexOf('Firefox') != -1) {
    return tta__sentence_delimiter;
  } else if (window.navigator.userAgent.indexOf('MSIE') != -1 || !!document.documentMode == true) {
    //IF IE > 10
    return tta__sentence_delimiter;
  } else {
    return tta__sentence_delimiter;
  }
}

/**
 * Change record button text.
 */
function ttaChangeRecordButtonText() {
  var record_btn = document.getElementById('tta__start__record');
  if (TTA.recordStatus == 'stop') {
    TTA.recordStatus = 'record';
    recognition.stop();
    localStorage.setItem('recordStarted', false);
    if (record_btn) record_btn.innerHTML = TTA.recordStartButtonContent();
  } else if (TTA.recordStatus == 'record') {
    if (localStorage.getItem('recordStarted') == null || localStorage.getItem('recordStarted') == 'false') {
      localStorage.setItem('recordStarted', true);
      recognition.start();
    }
    TTA.recordStatus = 'stop';
    if (record_btn) record_btn.innerHTML = TTA.recordStopButtonConten();
  }
}

/**
 *
 * @param {*} current_record_content_id
 * @returns
 */
function ttaGetCurrrentRecordingElement(current_record_content_id) {
  var current_recording_element = '';
  if (current_record_content_id == 'content_ifr') {
    current_recording_element = document.getElementById(current_record_content_id).contentWindow.document.body;
  } else {
    current_recording_element = document.getElementById(current_record_content_id);

    /**
     * If block editor is active and current_record_content_id is null then give alert.
     */
    if (document.getElementsByClassName('wp-block-post-title') && !current_recording_element) {
      return false;
    }
  }
  return current_recording_element;
}

/**
 * Capitalize String.
 */
function ttaCaptalizeString(string) {
  if (string[0] !== ' ') {
    return string[0].toUpperCase() + string.slice(1);
  } else {
    return ' ' + string[1].toUpperCase() + string.slice(2);
  }
}

/**
 * Listent/Pause/Resume content.
 */
function ttaListenCotentInDashboard(btn_id, content, listeningSettings) {
  /**
   * Stop recording before listening.
   */
  if (TTA.speechRecognitionIsActive && recognition) {
    recognition.stop();
  }
  if (!TTA.speechSynthesis) {
    TTA.displayApiMissing('', true);
  }
  localStorage.setItem('recordStarted', false);
  localStorage.setItem('current_play_btn_id', btn_id);
  ttaSetCurrentListeningContent();
  var text = localStorage.getItem('current_listening_content');
  ttaStartListening(btn_id, text, listeningSettings);
}
function ttaSetCurrentListeningContent() {
  var current_listening_content = '';
  var text = '';
  /**
   * Classsic editor
   */
  if (text_to_audio_obj.classic_editor_is_active) {
    /**
     * Get the title.
     */
    var title = document.getElementById('title').value;
    text = title + '. ';
    current_listening_content = document.getElementById('content_ifr').contentWindow.document.body;
    text += current_listening_content.innerText || current_listening_content.textContent;
    // is block editor active.
  } else if (document.getElementsByClassName('wp-block-post-title')) {
    // Block Editor Title
    current_listening_content += document.getElementsByClassName('wp-block-post-title')[0].innerText + '. ';
    // Content
    var blockEditorContent = document.getElementsByClassName('block-editor-block-list__layout');
    var _iterator2 = _createForOfIteratorHelper(blockEditorContent[0].children),
      _step2;
    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        child = _step2.value;
        // Get only innerText.
        if (child.getAttribute('id')) {
          current_listening_content += document.getElementById(child.getAttribute('id')).innerText;
        }
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
    text = current_listening_content;
  }
  localStorage.setItem('current_listening_content', text);
  // }
}

/**
 * Start Reading content
 */
function ttaStartListening(btn_id, content) {
  var listeningSettings = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;
  var listen_btn = document.getElementById(btn_id);
  TTA.utterence.text = content;
  var voices = speechSynthesis.getVoices();
  if (listeningSettings) {
    TTA.utterence.voice = voices.filter(function (voice, i) {
      return voice.name === listeningSettings.tta__listening_voice;
    })[0];
  } else {
    TTA.utterence.voice = voices[0];
  }
  TTA.utterence.volume = listeningSettings.tta__listening_volume ? listeningSettings.tta__listening_volume : 1; // From 0 to 1
  TTA.utterence.rate = listeningSettings.tta__listening_rate ? listeningSettings.tta__listening_rate : 1; // From 0.1 to 10
  TTA.utterence.pitch = listeningSettings.tta__listening_pitch ? listeningSettings.tta__listening_pitch : 2; // From 0 to 2
  TTA.utterence.lang = listeningSettings.tta__listening_lang ? listeningSettings.tta__listening_lang : 'en-US'; // It will be speaking language.

  if (TTA.listenStatus == 'listen') {
    //IMPORTANT!! Do not remove: Logging the object out fixes some onend firing issues.
    console.log(TTA.utterence);
    // Placing the speak invocation inside a callback fixes ordering and onend issues
    setTimeout(function () {
      speechSynthesis.speak(TTA.utterence);
    }, 0);
    TTA.timer = setTimeout(function pauseResumeTimer() {
      speechSynthesis.pause();
      //IMPORTANT!! Do not remove: Logging the object out fixes some onend firing issues.
      console.log(TTA.utterence);
      // Placing the speak invocation inside a callback fixes ordering and onend issues
      setTimeout(function () {
        speechSynthesis.resume();
      }, 0);
      TTA.timer = setTimeout(pauseResumeTimer, 10000);
    }, 10000);
    listen_btn.innerHTML = TTA.pauseButtonContent();
    listen_btn.setAttribute('title', 'Text To Audio : ' + TTA.pauseButtonText());
    TTA.listenStatus = 'pause';
  } else if (TTA.listenStatus == 'pause') {
    clearTimeout(TTA.timer);
    speechSynthesis.pause();
    listen_btn.innerHTML = TTA.resumeButtonContent();
    listen_btn.setAttribute('title', 'Text To Audio : ' + TTA.resumeButtonText());
    TTA.listenStatus = 'resume';
  } else if (TTA.listenStatus == 'resume') {
    listen_btn.innerHTML = TTA.pauseButtonContent();
    TTA.listenStatus = 'pause';
    listen_btn.setAttribute('title', 'Text To Audio : ' + TTA.pauseButtonText());
    speechSynthesis.resume();
    TTA.timer = setTimeout(function pauseResumeTimer() {
      speechSynthesis.pause();
      //IMPORTANT!! Do not remove: Logging the object out fixes some onend firing issues.
      console.log(TTA.utterence);
      // Placing the speak invocation inside a callback fixes ordering and onend issues
      setTimeout(function () {
        speechSynthesis.resume();
      }, 0);
      TTA.timer = setTimeout(pauseResumeTimer, 10000);
    }, 10000);
  }
}

/**
 * After ending reading the content.
 */
if (TTA.speechSynthesis && TTA.utterence) {
  TTA.utterence.addEventListener('end', function (event) {
    //speechSynthesis.cancel();
    clearTimeout(TTA.timer);
    var listen_btn = document.getElementById(localStorage.getItem('current_play_btn_id'));
    listen_btn.innerHTML = TTA.replayButtonContent();
    listen_btn.setAttribute('title', 'Text To Audio : ' + TTA.replayButtonText());
    TTA.listenStatus = 'listen';
  });
}

/**
 * Read the blog
 */
function ttaListenCotentInFrontend() {
  var content = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'Hellow World';
  var btn_id = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'wpa__listen_content';
  var listeningSettings = arguments.length > 2 ? arguments[2] : undefined;
  /**
   * Stop recording before listening.
   */
  if (TTA.speechRecognitionIsActive && recognition) {
    recognition.stop();
  }
  if (!TTA.speechSynthesis) {
    TTA.displayApiMissing(btn_id);
  }

  // Check if speechSynthesis is enabled or not?
  localStorage.setItem('recordStarted', false);
  localStorage.setItem('current_play_btn_id', btn_id);
  ttaStartListening(btn_id, content, listeningSettings);
}

/**
 * Get all textarea and start recording on focus event and stop recording on focusout event.
 */
Object.values(document.getElementsByTagName('textarea')).forEach(function (textarea, index) {
  var record_btn = document.getElementById('tta__start__record');
  /**
   * Start recording on focus event.
   */
  textarea.addEventListener('focus', function () {
    TTA.listenStatus = 'listen';
    var listen_btn = document.getElementById('wpa__listen_content');
    if (listen_btn) listen_btn.innerHTML = TTA.playButtonContent();
    if (listen_btn) listen_btn.setAttribute('title', 'Text To Audio : ' + TTA.playButtonText());
    /**
     * Start Recording.
     */
    if (textarea.getAttribute('id') === 'comment' || textarea.getAttribute('id') === 'tta__demo_text_for_play') {
      /**
       * Stop listening before recording.
       */
      if (TTA.speechSynthesis) {
        speechSynthesis.cancel();
      }
      if (!TTA.speechRecognitionIsActive) {
        TTA.displayApiMissing();
      }
      localStorage.setItem('current_recording_content_id', textarea.getAttribute('id'));
      ttaStartRecording(textarea.getAttribute('id'), localStorage.getItem('tta__sentence_delimiter'));
      TTA.recordStatus = 'record';
      if (record_btn) record_btn.innerHTML = TTA.recordStopButtonConten();
    }
  });

  /**
   * Stop recording on focusout event.
   */
  textarea.addEventListener('focusout', function () {
    // recognition.stop();
    // localStorage.setItem('recordStarted', false)
    // TTA.recordStatus = 'record';
    // record_btn.innerHTML = 'Start'
  });
});
window.tta = TTA;
/******/ })()
;